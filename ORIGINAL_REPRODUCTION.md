# 원본 HTML·모션 재현 기록 · 2026-09-16

검수 주소: <http://127.0.0.1:8018/original>. 사용자가 제공한 `MarketBrew App v188 copy 2.dc.html`과 같은 폴더의 25개 컴포넌트, `support.js`를 읽고 실행한 뒤 웹 빌드에 반영했다. 첫 React 구현과 Flutter 정적 실험을 원본 재현 완료로 취급하지 않는다.

현재는 **원본 디자인을 사용하는 로컬 예시 데이터 앱**이다. 새 화면의 실제 Kiwoom·DeepSeek 연결, 공개 배포, 디스콰이엇 등록은 미완료다. `/`에 있는 기존 API 연결 화면과 데이터 어댑터를 아직 합치지 않았다.

## 구현 선택과 보존 범위

사용자는 A안과 크로스 플랫폼을 요청했다. Flutter/Dart는 앞선 AI의 구현 선택이었으며 사용자가 특정한 언어가 아니었다. 이번에 실행 가능한 웹 원본을 제공받아, 언어를 반드시 Dart로 유지해야 하는지 선택 질문을 보냈다. 답변이 없는 상태에서 원본 재사용을 작업 가정으로 명시하고 진행했다. 이를 새 사용자 승인으로 기록하지 않는다.

HTML·CSS·JavaScript가 브라우저에서 실행되고 React가 원본 컴포넌트를 렌더링한다. 동일한 웹 코드를 여러 OS의 브라우저에서 사용할 수 있는 구조다. 실제 실행 검증은 Windows Chromium에서만 했다. Android·iOS 네이티브 패키지는 만들지 않았다. [Capacitor](https://capacitorjs.com/docs)는 향후 웹 코드를 앱으로 포장하는 선택지이며 현재 의존성이 아니다.

원본의 402×874 내부 화면, 화면 크기에 맞춘 축소, 여백, 글꼴, SVG, 사진, CSS 키프레임과 입력 처리 코드를 보존했다. 모바일에서 다른 배치로 재설계하는 반응형 구현은 추가하지 않았다. 사진 9개는 원본 브라우저 응답을 저장했고, JetBrains Mono는 Google Fonts가 반환한 문자 범위와 대체 글꼴 규칙까지 사용한다. Pretendard는 원본과 같은 전체 글꼴이다.

| 경로 | 역할 |
|---|---|
| `original/` | 원본 26개 HTML, 렌더러, 출처·사진 해시 목록. 사용자 Downloads 원본은 수정하지 않음 |
| `tools/build-original.mjs` | 신뢰하는 로컬 원본의 클래스와 템플릿을 빌드 입력으로 변환. 원본 변경 시 필요한 경계나 사진 해시가 다르면 실패 |
| `src/original/renderer.js` | 원본 레이아웃·수명주기를 유지하는 렌더러 연결 |
| `src/original/demo.js` | 제외 기능, 예시 표시, 기기 내 저장, 예시 대화만 적용 |
| `public/original-assets/` | 로컬 사진·JetBrains Mono 글꼴·라이선스 |
| `server/app.py`의 `/original` | 빌드 HTML과 응답마다 다른 스타일 nonce 제공 |

루트 HTML SHA-256: `b7bc20f2e13cb5862facbeb780fe4b59a21462f081870859aecf6a2f80a34005`. 나머지는 [source-manifest.json](original/source-manifest.json)에 기록했다.

## 의도적으로 바꾼 동작

- 로그인·가입, 알림, 커뮤니티 탭·인기글·투표·게시물·커뮤니티 프로필과 진입점을 제외했다. 개인 이름 설정은 로그인 없는 기기 설정으로 제공한다.
- 상단에 `디자인 데모 · 예시 시세·분석`을 표시한다. 움직이는 숫자, 분석 점수, 수집 실적 문구도 원본의 예시이며 실제 수집 결과가 아니다.
- 온보딩 완료, 이름, 관심 ETF·테마·그룹, 선택 그룹, 스토리 읽음 상태를 `edge.original.preferences.v1`에 저장한다. 저장 실패를 화면에 표시한다. 서버 계정이나 기기 간 동기화는 없다.
- 원본의 `window.claude.complete` 호출을 기본 데모에서 실행하지 않는다. 대화는 원본 UI와 예시 응답을 사용하고 답변 앞에 예시임을 표시한다. DeepSeek로 바꿔 연결했다는 뜻이 아니다.

`/original?fixture=original`은 위 변경을 적용하지 않는 내부 비교용 경로다. 원본 그대로의 화면과 렌더러를 비교하기 위한 것이며 공개 제품 진입점으로 사용하지 않는다.

## 페이지 데이터와 다음 연결 경계

| 화면 | 새 화면이 현재 읽는 데이터 | 실제 연결 시 필요한 검증 |
|---|---|---|
| 온보딩·관심·그룹 | 원본 ETF 키 목록, `watch`, `themeWatch`, `wGroupList`, `watchGroup` | 사용자 선택을 보존하면서 검증된 국내 상장 종목으로 대응 |
| 홈·검색·ETF 상세 | 원본 `ETFS`, `ART`, 예시 가격·차트·비중 | 기존 `Catalog`·`Instrument`·`Candle`·`Holding` 응답과 매핑, 단위·기준 시각·자료 없음 표시 |
| 테마·이슈·스토리 | 원본 분류·문구·예시 점수 | 실제 원자료·출처·분석 시점 확인. 예시 전망을 실시간 판단으로 옮기지 않음 |
| AI 대화 | 화면 상태와 원본 예시 응답 | 서버 분석 요청·실패·예산 상태를 연결하고 DeepSeek→허용 도구 왕복 검증 |

예를 들어 `AXAI`는 이 디자인의 내부 키다. 이 문자열을 Kiwoom 종목코드로 보내면 안 된다. 종목명만으로 추측하지 말고 공식 목록에서 국내 상장 종목을 확인해 대응해야 한다. 기존 API의 모델과 안전 경계는 [ARCHITECTURE.md](ARCHITECTURE.md)에 있다.

## 보안 경계

원본 `support.js`의 외부 React 로더, 런타임 코드 평가, 편집기 메시지 연결을 패키지 렌더러에서 제거했다. 로컬 원본 클래스는 일반 JavaScript 모듈로 빌드한다. 템플릿 해석기는 사용하지만 `eval`이나 `Function` 생성자로 스크립트를 실행하지 않는다. 사용자가 보낸 문자열을 컴포넌트 소스로 받는 API도 없다.

스크립트·연결·글꼴은 CSP에서 같은 출처로 제한한다. 원본 CSS를 삽입할 때 서버가 발급한 nonce를 사용하고, 기존 원본의 인라인 스타일 속성은 허용한다. 브라우저에 금융·LLM 키를 넣지 않았다. 이것은 해당 경계 검사 결과이며 원본 모든 동작에 대한 보안 감사 완료를 뜻하지 않는다.

## 검증 결과와 한계

최신 빌드 성공. 도메인 4개, 서버 14개, 브라우저 11개가 모두 통과했다. 브라우저 11개 중 6개는 새 원본 화면용이며 제외 기능, 온보딩·저장·그룹, 저장 실패, 예시 대화, CSP, 실제 애니메이션 시간 진행을 검사한다. 생략된 테스트는 없다.

원본과 패키지를 별도 브라우저 페이지에서 실행해 **25개 상태·44개 시점**을 비교했다. 온보딩 3종의 시작·중간·종료와 반복, 테마·ETF 선택/검색/빈 상태, 홈·관심, ETF 3탭, 탐색·테마·이슈, 메뉴와 분석 시트를 포함한다. 모든 Figma 상태나 스크롤 아래의 전체 내용을 검사한 것은 아니다.

| 항목 | 결과 |
|---|---|
| 470×950, DPR 1에서 텍스트와 모션 메타데이터 | 25/25 일치. 키프레임·duration·delay·easing·반복·방향 포함 |
| 픽셀 비교 | 44개 중 43개 완전 일치. 광범위한 마스크·허용 오차로 차이를 숨기지 않음 |
| 남은 차이 | 테마 화면 사진, 16,494픽셀에 차이. 채널 차이가 20/255를 넘는 픽셀은 137개, 전체 평균 채널 차이 0.098371/255 |
| 원본 자체의 독립 반복 촬영 | 44/44 완전 일치. 남은 차이를 일반적인 랜덤 노이즈로 판정하지 않음 |
| 사진 추가 진단 | 원본 응답과 로컬 파일 해시 일치, 사진 위치·크기와 배경 관련 스타일 일치. 별도 단일 화면 비교에서도 작은 차이가 남아 원인 확정 전 |
| 패키지 브라우저 오류 | 비교 중 0건. 원본은 초기 미해석 SVG 속성 등의 오류 16건 기록 |
| 기본 데모의 외부 요청 | 온보딩→홈→검색→ETF 흐름에서 0건 |
| 360/402/430px 홈 | 문서 가로 넘침 없음. 실제 휴대폰 터치·Safari·Android·iOS는 미검증 |

따라서 **전체 화면·모든 플랫폼의 픽셀 동일성 완료로 판정하지 않는다.** 요청에 따라 제외한 영역의 픽셀 비교와 기능 검사는 분리한다. 위 43/44는 원본 비교 모드의 결과이며, 제외 영역까지 기본 데모가 원본과 같다는 뜻이 아니다.

초기 비교 중 원본의 지연 로딩 컴포넌트·글꼴이 준비되기 전에 촬영한 문제, 시험 결과 폴더 충돌, 반복 촬영 파일을 일반 원본으로 집계한 문제를 수정했다. 현재 비교는 모든 컴포넌트와 이미지 디코딩을 기다리고, JS 예시 시세 타이머만 멈춘 뒤 실제 CSS 애니메이션을 같은 시각으로 이동시킨다. 애니메이션이 자연스럽게 진행되는지는 별도 클릭 시험에서 확인한다.

증거: `artifacts/original-fidelity/checks.json`, `frames.json`, `pixels.json`, `pixels-source-repeat.json`, 각 PNG와 오류 기록. [홈 검수 이미지](artifacts/original-fidelity/demo-home.png). 시험 산출물은 Git에서 제외한다.

재실행은 원본 폴더를 별도 터미널에서 `python -m http.server 8020 --bind 127.0.0.1`로 제공하고, 앱을 빌드·실행한 뒤 아래를 사용한다. 픽셀 진단에는 Python Pillow가 필요하며 서버의 운영 의존성은 아니다. `compare-original.py`는 차이가 남으면 종료 코드 1을 반환한다. 현재 완전한 픽셀 동일성 검사는 이 상태이며 기능 검사 통과와 구분한다.

```powershell
$env:SOURCE_REPEAT='1'
node tests/original-fidelity.mjs
python tools/compare-original.py
Remove-Item Env:SOURCE_REPEAT
```

빌드 산출물의 원본 JS와 공유 React JS는 합계 약 940KB, gzip 추정 합계 약 256KB다. 이는 실제 HTTP 전송량 측정이 아니다. Vite의 500KB 청크 경고가 있으며 전체 Pretendard WOFF2 자산은 약 6.9MB다. 현재는 시각 보존을 우선했고 가벼운 공개 배포를 위한 글꼴 최적화·캐시·압축은 남아 있다.

## 사용자가 검수할 완료 조건

원본 렌더링을 `R(s,t,v)`, 새 구현을 `I(s,t,v)`라 하면, 같은 상태 `s`, 시간 `t`, 화면 크기 `v`에서 배치·문구·모션 설정을 대조한다. 이번 픽셀 잔차는 `#{p : R(p) ≠ I(p)}`로 세었고 남은 사진 차이를 0으로 보고하지 않았다. 실제 데이터 연결 후에는 화면 비교와 별도로 종목 대응·가격 단위·기준 시각·결측·연결 장애를 다시 검증해야 한다.

이해도 확인 질문 하나: **“화면이 원본과 똑같이 움직여도 실제 시세 연결이 끝났다고 말할 수 없는 이유와, 이를 확인할 테스트 하나를 설명해 주세요.”** 수학적 입력·출력·불변조건으로 기술하거나 필요한 용어를 아는 중학생에게 설명하는 방식 중 하나면 된다. 사용자의 답변을 아직 관찰하지 않았으므로 이해도 도달 판정은 보류한다.
