# 원본 재현 정정 · 2026-09-16

**후속 정정:** 사용자가 `MarketBrew-clean/MarketBrew App v188 copy 2.dc.html`을 제공했다. 실행 가능한 26개 원본 컴포넌트와 모션을 확보하여 `/original` 웹 검수본에 재사용했다. Flutter/Dart가 필수라는 응답은 받지 않았고 원본 웹 재사용을 작업 가정으로 설명한 뒤 진행했다. [현재 구현·검증·한계](../ORIGINAL_REPRODUCTION.md)가 아래의 초기 Flutter 계획보다 우선한다.

- [x] 원본 HTML·렌더러·사진·글꼴 확보 및 해시 기록
- [x] 원본 CSS 모션과 입력 동작 패키징, 제외 기능과 기기 저장 적용
- [x] 25개 상태·44개 프레임 비교: 텍스트/모션 일치, 픽셀 43/44 완전 일치
- [x] 원본 화면 브라우저 6개, 기존 포함 전체 로컬 29개 검사 통과
- [ ] 테마 사진의 미세한 픽셀 차이 1프레임 원인 해결
- [ ] 새 화면과 기존 API 매핑, 실제 금융·LLM 통합 검증
- [ ] 모든 포함 상태·실제 휴대폰·대상 플랫폼 검수, 공개 배포

이하 내용은 원본 소스를 받기 전의 조사·정적 실험 기록이다.

사용자는 기존 React 웹 데모의 디자인·모션 불일치를 지적하고 크로스 플랫폼 구현을 요청했다. 기능 검사 23개 통과는 원본 재현 통과가 아니다. 현재 UI는 디자인 검수 실패 상태다.

## 실행 방향

- Flutter/Dart로 화면을 다시 구현한다. 우선 웹·Android·iOS를 대상으로 하며 플랫폼 질문에 다른 답변이 오면 반영한다.
- 원본 Figma의 치수·줄바꿈·글꼴·색·아이콘·그림자·유리 효과·화면 상태를 따른다. 임의의 화면 재설계와 그래픽 삭제를 하지 않는다.
- 국내 ETF, 로그인·알림 제외를 유지한다. 커뮤니티는 최신 지시로 복원했다. 제외 영역 외의 배치를 임의 변경하지 않는다.
- 데모 값과 실제 값은 구분한다. 원본 예시 점수·전망은 디자인 재현용 fixture에서 표시할 수 있지만 실시간 분석인 것처럼 제공하지 않는다.
- 화면 구현은 `apps/edge_flutter`, 금융·AI API는 기존 `apps/edge/server`를 사용한다. 공개 배포와 실제 API 검증 상태는 변하지 않는다.

## 다시 확인한 원본

- 첫 온보딩 `64:398`: 언론사 띠, 통계 카드, AI 배지, 원본 문구·배치가 기존 구현과 다름.
- 두 번째 온보딩 `64:575`: 5단계 스티커, 유리 효과, 요인 칩 배열을 복원해야 함.
- 홈 `63:116`: 요약 카드의 전망 눈금·스티커·그룹과 행의 치수에 큰 차이가 있음.
- 앱 페이지 `0:1`의 프로토타입 반응 2,086개를 확인했으나 지정된 transition은 0개.
- 온보딩 3개 프레임의 `get_motion_context(recursive=true)` 결과가 모두 `nodes: []`. 이것으로 파일 전체에 모든 종류의 모션이 없다고 단정하지 않는다.
- 원래 움직임의 실행 URL/소스 경로/프로토타입 링크를 요청했다. 시간·easing·궤적을 추정해 원본과 같다고 보고하지 않는다.

## 완료 판정

1. 대표 화면부터 동일 viewport·fixture·글꼴로 렌더링한다. 원본과 겹친 이미지 및 차이를 확인하고 위치·크기·줄바꿈 차이를 해결한다.
2. 제외 영역은 비교에서 명시적으로 분리한다. 렌더러의 글꼴 안티앨리어싱 차이를 숨기려고 광범위한 마스크를 사용하지 않는다.
3. 원본 motion 값을 확보한 동작은 시작·중간·종료 프레임, duration·easing·반복·입력 반응을 대조한다.
4. 원본 motion 자료가 없는 항목은 미검증으로 남긴다. 빌드 성공이나 클릭 가능만으로 시각·모션 통과 처리하지 않는다.
5. 웹에서 실제 실행·화면/동작 대조를 통과한 뒤, Android·iOS도 각각 빌드/기기 증거를 구분한다. Windows에서 iOS 검증 완료를 주장하지 않는다.

## 현재 상태

- [x] 사용자 정정·이전 검증의 한계 기록
- [x] 원본 정적 구조와 프로토타입 반응 재조회
- [x] Flutter 3.47.2 / Dart 3.13.2 실행 환경 준비
- [x] 첫 3개 온보딩 정적 화면·테마 선택 위젯, 웹 빌드와 원본/실제 이미지 비교
- [ ] 온보딩·홈부터 원본 위젯 구현·시각 대조
- [ ] 포함 화면 전체와 개인 상태·API 연결
- [ ] 모션 원본 확보·재현·시간별 검수
- [ ] 대상 플랫폼별 실행 증거

현재 Flutter URL은 `/flutter/`의 디자인 검수본이다. 첫 3개 화면과 테마 미선택 화면의 정적 비교를 실행하며, 테마 이후 제품 동선·기존 앱의 전체 기능 이식은 미완료다. 3개 온보딩 일러스트레이션에 원본 정적 PNG를 사용했고, 이는 모션 구현을 대체하지 않는다. 정적 비교에서도 픽셀 차이가 남아 있으므로 '동일 재현 완료'로 판정하지 않았다.
