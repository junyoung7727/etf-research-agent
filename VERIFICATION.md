# EDGE 검증 기록 · 2026-09-16

현재 판정: **로컬 기능 동작 확인. 원본 HTML을 재사용한 `/original` 검수본 추가. 공개 출시 조건 미충족.** 최신 원본 비교는 25개 상태의 텍스트·모션 일치, 44개 프레임 중 43개 픽셀 일치다. 남은 사진 차이와 비교 조건은 [원본 재현 기록](ORIGINAL_REPRODUCTION.md)에 명시했다. Flutter 정적 실험은 이전 기록으로 남긴다.

## 실행한 검사

| 검사 | 실제 결과 | 검증한 범위 |
|---|---|---|
| `npm run build` | PASS | TypeScript 검사 및 두 화면 프로덕션 번들. 원본 JS + 공유 JS gzip 추정 약 256KB, 글꼴·이미지 별도. 500KB 청크 경고 있음 |
| `npm test` | 4/4 PASS, skipped 0 | 등락률·결측, 관심 중복/상한, 재정렬, 로컬 상태 검증 |
| `.venv/Scripts/python -m unittest server.test_contracts -q` | 14/14 PASS | 단위·API·출처·도구 허용 범위, 단일 토큰 갱신, 연결 끊김 상태, 분석 중복/예산/SQLite 재개 |
| `npm run test:e2e` | 11/11 PASS, skipped 0 | 기존 5개 + 원본 화면 6개. 최신 빌드와 재시작 서버에서 실행 |
| `npm audit --omit=dev` | 앞선 실행에서 보고된 취약점 0 | 이번 원본 반영에서는 의존성을 추가하지 않았으며 감사 명령을 다시 실행하지 않음. 전체 보안 평가가 아님 |

합계 29개 로컬 검사가 통과했다. 새 원본 화면 6개의 범위와 별도 시각·모션 대조는 [원본 재현 기록](ORIGINAL_REPRODUCTION.md)에 있다. 아래는 기존 API 연결 화면의 5개 브라우저 검사와 앞선 진단 기록이다:

1. 온보딩의 테마·ETF 선택 → 홈 → 분석 근거 시트 → 차트 60거래일 → 전체 구성 27.01% 잔여 → 관심 목록 새로고침 보존. 페이지 JavaScript 예외 없음.
2. 새 그룹 → 종목코드 검색 → 그룹 담기 → 제거·되돌리기 → 그룹 삭제 취소. 다른 브라우저의 관심 목록은 비어 있음.
3. 360/402/430px에서 상세 직접 접근·가로 넘침 없음, 탐색 → 테마, 이슈 → 출처 열기.
4. API 503에서 오류와 재시도 표시. 공급자 오류를 데모 값으로 대체하지 않음.
5. 관심 ETF를 첫 위치에서 마지막 위치로 끌어서 이동한 뒤 새로고침해도 순서 보존.

스크린샷: `test-results/edge-home.png`, `test-results/edge-chart.png`. 실제 이미지를 열어 확인했다. 잘못 연결된 뒤로가기/관심 아이콘을 원본 Figma 자산으로 수정했다. 모든 Figma 변형에 대한 픽셀 비교는 하지 않았다. 관심 재정렬은 드래그와 위/아래 버튼으로 제공한다. 드래그는 Chromium 마우스로 검증했으며 실제 휴대폰 터치는 별도 확인이 필요하다.

최신 코드로 로컬 서버를 재시작한 뒤 브라우저 5개 흐름을 다시 통과했다. 상태·카탈로그·ETF API·상세 직접 링크는 200, 커뮤니티·주문 API·`.env`·SQLite 파일 접근은 404를 확인했다. 현재 서버는 `http://127.0.0.1:8018`에서 DEMO·ETF 6종을 제공한다. 실행 데이터·세션 키·의존성·빌드·시험 산출물의 Git 제외와 문서의 로컬 링크도 확인했다.

초기 브라우저 검사는 Playwright 브라우저 바이너리가 없어 실행 실패했다. Chromium 설치 후 재실행하여 통과했다. Python 테스트는 현재 Starlette의 httpx TestClient 사용 중단 예고 경고가 있으며, 이 경고로 생략된 테스트는 없다.

## 외부 경계에서 확인한 사실

- 공식 Spec MCP로 `ka40004`, `ka10081`, `ka10001`, `au10001` 계약을 읽었다. Exec MCP `kiwoom_help`로 CLI 옵션을 확인했으며 금융 API 쿼리는 실행하지 않았다.
- 읽기용 Kiwoom 저장소 기준 커밋: `953e5dbff123f437ab4d11a78a95191a685eb51f`.
- 연결된 TinyFish 도구에서 검색 성공. TIGER 상품 페이지 추출은 timeout. 한국은행 페이지 추출은 성공하여 `results[].text`와 `final_url` 응답을 확인하고 해당 계약 및 비허용 최종 URL 회귀 시험을 추가했다.
- 위 도구 확인은 **이 앱의 서버용 키로 DeepSeek→MCP 왕복이 성공했다는 증거가 아니다.**

## 아직 실행하지 못한 검사·남은 구현

| 항목 | 상태·이유 |
|---|---|
| 새 원본 화면과 기존 API 모델 매핑 | 미완료 — `/original`은 명시된 원본 예시 데이터. 내부 키를 실제 종목코드로 검증해 대응해야 함 |
| 전체 화면·모든 플랫폼 디자인 동일성 | 미완료 — 25상태·44프레임만 비교, 사진 차이 1프레임 남음. 네이티브 빌드·실제 휴대폰 미검증 |
| Kiwoom 실제 인증·장중 체결·토큰 만료·WS 재구독 | NOT_RUN — 이 앱의 자격증명·허용 IP가 미설정 |
| DeepSeek와 두 MCP 서버의 실제 왕복·재시작 인증 | NOT_RUN — 서버 전용 키·모델·MCP 명령·예산 미설정 |
| LLM 주장과 근거의 논리적 일치, 공격 문서 통합 평가 | NOT_RUN — 결정적 거부 검사만 실행. 인용 일치가 사실성을 입증하지 않음 |
| 운용사 편입 비중·보수·분배금, 매크로·수급·밸류 실데이터 | 미구현 — live에서는 자료 없음. 예시로 자동 보완하지 않음 |
| 뉴스 이슈의 지속 수집·출처 갱신 | 미구현 — 현재는 분석 요청 중 제한된 출처 검색·추출 코드와 예시 화면 |
| 전망 점수·순위·테마 임계값·베타 스토리 | 사용자 정책/포함 여부 미결정. 보류 표시·베타 미구현 |
| 20명·10분 부하, 장중 100건의 수신→화면 p95, 30초 복구 실측 | NOT_RUN — 일부 단위 시험으로 대체 판정하지 않음 |
| 별도 배포 환경의 백업 복원·TLS·고정 송신 IP | NOT_RUN — 로컬 SQLite 재개 시험만 PASS |
| 시세 공개 제공 권한, 외부 URL, 디스콰이엇 등록 | 미완료 — 운영 환경·적용 권한 확인 필요 |

기존 36개 검증 ID 전체를 PASS로 바꾸지 않는다. 현재 증거는 G1/G2/G4/G5의 일부 로컬 조건이며 G3·G6의 실제 통합 조건은 남아 있다.
